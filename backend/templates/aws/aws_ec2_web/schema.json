{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AWS EC2 Web Application Server",
  "description": "Standalone Linux web server running Nginx with automated startup configuration, security group, and Elastic IP",
  "type": "object",
  "required": ["server_name", "instance_type"],
  "properties": {
    "server_name": {
      "type": "string",
      "title": "Server Name",
      "default": "web-frontend",
      "description": "Hostname and Name tag applied to the EC2 instance"
    },
    "instance_type": {
      "type": "string",
      "title": "Compute Instance Type",
      "default": "t3.micro",
      "enum": ["t3.micro", "t3.small", "t3.medium", "t3.large", "t3.xlarge"],
      "description": "AWS EC2 compute tier sizing"
    },
    "allocated_storage_gb": {
      "type": "number",
      "title": "EBS Storage (GB)",
      "default": 20,
      "minimum": 10,
      "maximum": 500,
      "description": "Root volume storage capacity in Gigabytes"
    },
    "allowed_ssh_cidr": {
      "type": "string",
      "title": "Allowed SSH CIDR",
      "default": "",
      "description": "Permitted IPv4 network block for SSH administrative access. Leave blank to disable inbound SSH."
    },
    "vpc_id": {
      "type": "string",
      "title": "VPC Identifier",
      "default": "",
      "description": "Target VPC (leave empty to deploy into default VPC)"
    },
    "subnet_id": {
      "type": "string",
      "title": "Subnet Identifier",
      "default": "",
      "description": "Target Subnet (leave empty to select default subnet)"
    }
  }
}
