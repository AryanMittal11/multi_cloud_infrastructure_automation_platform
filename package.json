{
  "name": "multi-cloud-infrastructure-automation-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-Cloud Infrastructure Automation Platform managing cloud infrastructure across AWS, Azure, and GCP",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:worker": "npm --prefix backend run worker:dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "test:backend": "npm --prefix backend run test"
  },
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "keywords": [
    "multi-cloud",
    "infrastructure-automation",
    "terraform",
    "aws",
    "azure",
    "gcp",
    "devops"
  ],
  "author": "Multi-Cloud Platform Team",
  "license": "MIT"
}
